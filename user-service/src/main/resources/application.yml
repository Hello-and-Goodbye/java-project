spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
      config:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        file-extension: yaml
  # 可选：从 Nacos 配置中心拉取 user-service.yaml，本地无该配置时不阻断启动
  config:
    import: "optional:nacos:user-service.yaml"
